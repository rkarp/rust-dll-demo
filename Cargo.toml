[package]
name = "rust-dll-init"
version = "0.1.0"
authors = ["Rolf Karp <rolf.karp@gmail.com>"]
publish = false

[dependencies]
winapi = { version = "0.3.6", features = ["consoleapi"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = true
